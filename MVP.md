# 受控访客互动墙 MVP

更新日期：2026-09-14

## 用户与问题

访问 Hresh赫什个人网站的访客，希望留下简短反馈、名片或贴纸；站长希望保留互动感，但不承担匿名实时公共画布带来的垃圾内容、图片托管、持续审核和不可控流量成本。

## 输入 → 处理 → 输出

- 输入：140 字以内留言，或受限字段的自我介绍卡、白名单贴纸；管理员创建的投票可接受访客投票。
- 处理：公开写入统一经过 Edge Function，执行 Origin、蜜罐与填写时长、字段白名单、内容去重、IP/设备/全站限流和待审核队列上限检查；投稿进入待审核区。
- 输出：管理员审核通过后，内容才进入公开白板；访客只读取最多 200 张已发布卡片和聚合票数。

## 成功标准

- 公开客户端不能直接写数据库、读取待审核内容、投票身份哈希或管理员名单。
- 投稿失败不会在页面上显示为已发布；成功时明确提示“等待审核”。
- 远端共享涂鸦、访客图片和访客创建投票不进入 MVP。
- 公开页面不依赖匿名登录，不订阅全表 Realtime，数据规模和成本有明确上限。
- 站长可在 Supabase Table Editor 完成发布、拒绝和撤回。
- 不依赖第三方验证码；即使启发式检查被绕过，单来源、全站和待审核队列配额仍限制最大损失。

## 本版本不做

- 独立审核后台、举报与申诉系统。
- 访客上传图片或文件。
- 永久共享涂鸦和卡片自由拖拽。
- 访客创建投票。
- 秒级实时同步；公开内容每 60 秒刷新一次。

## 失败信号

- 审核量持续超过站长可处理范围。
- 大量正常访客因限流无法投稿。
- 投稿功能很少使用，却持续增加运维成本。
- 需要频繁进入数据库手工修复状态。

出现失败信号时，优先收紧为只读展示，而不是扩建社区系统。
